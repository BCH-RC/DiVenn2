import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(
    org.Pfalciparum.eg,
    org.Pfalciparum.eg_dbconn,
    org.Pfalciparum.eg_dbfile,
    org.Pfalciparum.eg_dbschema,
    org.Pfalciparum.eg_dbInfo,

    org.Pfalciparum.egORGANISM
)

